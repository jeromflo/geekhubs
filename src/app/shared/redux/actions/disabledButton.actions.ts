import { createAction } from '@ngrx/store';

export const disabledButton = createAction(
    '[disabledButton] disabledButton'
);
export const showButton = createAction(
    '[disabledButton] showButton'
);

